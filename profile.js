const db=window.supabaseClient;let currentUser=null,profile=null;document.addEventListener('DOMContentLoaded',async()=>{try{const{data:{user}}=await db.auth.getUser();if(!user){location.href='login.html';return}currentUser=user;if(typeof checkMiningIncome==='function')await checkMiningIncome();await initializeProfile()}catch(e){console.error(e)}});async function initializeProfile(){await Promise.all([loadProfile(),loadStatistics(),loadNotifications()])}async function loadProfile(){const{data,error}=await db.from('profiles').select('*').eq('id',currentUser.id).single();if(error){console.error(error);return}profile=data||{};const set=(id,v)=>{const el=document.getElementById(id);if(el)el.textContent=v};set('userName','Welcome, '+(profile.fullname||'User'));set('fullName',profile.fullname||'User');set('email',profile.email||currentUser.email||'No Email');set('walletBalance','UGX '+Number(profile.wallet_balance||0).toLocaleString());set('membership',profile.membership||'Standard');set('kycStatus',profile.kyc_status||'Not Verified');set('accountStatus',profile.account_status||'Active');if(profile.avatar_url){const avatar=document.getElementById('avatar');if(avatar)avatar.src=profile.avatar_url}}async function loadStatistics(){if(!profile)return;const{data:machines}=await db.from('user_machines').select('id').eq('user_id',currentUser.id).eq('status','active');const{data:team}=await db.from('referrals').select('id').eq('referrer_id',currentUser.id);const set=(id,v)=>{const el=document.getElementById(id);if(el)el.textContent=v};set('activeMachines',machines?.length||0);set('teamMembers',team?.length||0);set('referralBonus','UGX '+Number(profile.total_referral_bonus||0).toLocaleString());const code=profile.referral_code||'';const codeEl=document.getElementById('referralCode');const linkEl=document.getElementById('referralLink');if(codeEl)codeEl.value=code;if(linkEl)linkEl.value=location.origin+'/register.html?ref='+encodeURIComponent(code)}async function loadNotifications(){const{data,error}=await db.from('user_notifications').select('id').eq('user_id',currentUser.id).eq('is_read',false);if(error)console.error(error);const total=data?.length||0;['notifyCount','notificationCount'].forEach(id=>{const el=document.getElementById(id);if(el)el.textContent=total})}setInterval(async()=>{if(!currentUser)return;await loadProfile();await loadStatistics();await loadNotifications()},30000);async function copyText(value,message){if(!value)return;try{await navigator.clipboard.writeText(value);toast(message)}catch(e){console.error(e)}}function toast(message){const t=document.createElement('div');t.className='mdh-toast';t.textContent=message;document.body.appendChild(t);requestAnimationFrame(()=>t.classList.add('show'));setTimeout(()=>{t.classList.remove('show');setTimeout(()=>t.remove(),220)},1800)}document.getElementById('copyReferral')?.addEventListener('click',()=>copyText(document.getElementById('referralCode')?.value,'Referral code copied'));document.getElementById('shareReferral')?.addEventListener('click',async()=>{const link=document.getElementById('referralLink')?.value||'';if(!link)return;if(navigator.share){try{await navigator.share({title:'Marathon Digital Hub',text:'Join my Marathon Digital Hub team.',url:link})}catch(e){if(e.name!=='AbortError')console.error(e)}}else await copyText(link,'Referral link copied')});document.getElementById('changePhoto')?.addEventListener('click',()=>document.getElementById('photoInput')?.click());document.getElementById('photoInput')?.addEventListener('change',uploadPhoto);async function uploadPhoto(e){const file=e.target.files?.[0];if(!file)return;const name=currentUser.id+'-'+Date.now();const{error}=await db.storage.from('machine-images').upload(name,file,{upsert:true});if(error){toast(error.message);return}const{data}=db.storage.from('machine-images').getPublicUrl(name);await db.from('profiles').update({avatar_url:data.publicUrl}).eq('id',currentUser.id);const avatar=document.getElementById('avatar');if(avatar)avatar.src=data.publicUrl;toast('Profile photo updated')}document.getElementById('logoutBtn')?.addEventListener('click',async()=>{await db.auth.signOut();location.href='login.html'});