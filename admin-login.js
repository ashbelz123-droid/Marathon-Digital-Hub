const db=window.supabaseClient;
if(localStorage.getItem("admin_logged_in")==="true") window.location.href="admin-user-machines.html";
document.getElementById("loginForm").addEventListener("submit",async e=>{e.preventDefault();const email=document.getElementById("email").value.trim().toLowerCase(),password=document.getElementById("password").value,btn=document.getElementById("loginBtn"),msg=document.getElementById("message");btn.disabled=true;btn.innerHTML="Logging in...";msg.className="message loading";msg.innerHTML="Please wait...";try{const auth=await db.auth.signInWithPassword({email,password});if(auth.error)throw auth.error;const {data:admin,error}=await db.from("admins").select("id,fullname,email,role").eq("email",email).maybeSingle();if(admin&&["admin","super_admin"].includes(admin.role)){localStorage.setItem("admin_logged_in","true");localStorage.setItem("admin_name",admin.fullname||email);localStorage.setItem("admin_role",admin.role);localStorage.setItem("admin_id",admin.id);msg.className="message success";msg.innerHTML="Login successful...";setTimeout(()=>window.location.href="admin-user-machines.html",500);return}await db.auth.signOut();throw new Error("This account is not authorized as an MDH administrator.")}catch(err){btn.disabled=false;btn.innerHTML="Login";msg.className="message error";msg.innerHTML=err.message||"Invalid email or password."}});