/* Marathon Digital Hub — Admin User Machines V4
 * Uses the same bridge-token authentication as admin-login.js.
 */
(() => {
  "use strict";
  const db=window.supabaseClient;
  if(!db)return console.error("Supabase client missing");
  const $=id=>document.getElementById(id);
  const state={users:[],plans:[],machines:[],user:null,filter:"all",editing:null};
  const money=n=>`UGX ${Number(n||0).toLocaleString()}`;
  const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;","\"":"&quot;"}[c]));
  const val=(id,f="")=>$(id)?.value??f; const setVal=(id,v)=>{if($(id))$(id).value=v??""};
  const show=id=>$(id)?.classList.remove("hidden"); const hide=id=>$(id)?.classList.add("hidden");
  const today=()=>new Date().toISOString().slice(0,10); const dateOnly=v=>v?new Date(v).toISOString().slice(0,10):""; const iso=v=>v?new Date(`${v}T00:00:00`).toISOString():null;

  function toast(message,type="success"){const e=$("toast"),t=$("toastText");if(!e||!t)return;t.textContent=message;e.className=`toast show ${type}`;e.style.background=type==="error"?"#ff4d4f":"#00b95e";clearTimeout(window.__toast);window.__toast=setTimeout(()=>e.classList.remove("show"),3200)}
  async function safe(fn,msg=""){try{$("loadingScreen")?.classList.remove("hidden");const r=await fn();if(r?.error)throw r.error;if(msg)toast(msg);return r}catch(e){console.error(e);toast(e?.message||"Operation failed","error");return null}finally{$("loadingScreen")?.classList.add("hidden")}}

  function adminAuth(){
    const token=localStorage.getItem("admin_bridge_token");
    const logged=localStorage.getItem("admin_logged_in")==="true";
    if(!token||!logged){location.replace("admin-login.html");return false}
    return true;
  }

  async function loadUsers(){const {data,error}=await db.from("profiles").select("*").order("created_at",{ascending:false});if(error)throw error;state.users=data||[];renderUsers();updateOverview()}
  async function loadPlans(){const {data,error}=await db.from("machines").select("*").order("display_order",{ascending:true});if(error)throw error;state.plans=data||[];fillMachineSelect();updateOverview()}
  function updateOverview(){if($("totalUsers"))$("totalUsers").textContent=state.users.length;if($("activeUsers"))$("activeUsers").textContent=state.users.filter(u=>!u.is_frozen&&u.account_status!=="suspended").length;if($("totalMachines"))$("totalMachines").textContent=state.plans.length;if($("vipMachines"))$("vipMachines").textContent=state.plans.filter(m=>m.is_vip).length}

  function renderUsers(list=state.users){const box=$("usersList");if(!box)return;if(!list.length){box.innerHTML='<div class="emptyCard"><h3>No Users Found</h3><p>Try another name, phone, email or referral code.</p></div>';return}box.innerHTML=list.map(u=>`<div class="userCard ${state.user?.id===u.id?"active":""}" data-user="${esc(u.id)}"><div class="userLeft"><img class="userAvatar" src="${esc(u.avatar_url||"images/default-avatar.png")}" alt=""><div class="userInfo"><h3>${esc(u.fullname||"Unnamed User")}</h3><p>${esc(u.phone||"No phone")}</p><p>${esc(u.email||"No email")}</p></div></div><div class="userRight"><span class="badge membershipBadge">${esc(u.membership||"Standard")}</span><span class="userStatus ${u.is_frozen?"frozen":u.account_status==="suspended"?"suspended":""}">${u.is_frozen?"Frozen":esc(u.account_status||"Active")}</span></div></div>`).join("");box.querySelectorAll("[data-user]").forEach(c=>c.onclick=()=>selectUser(c.dataset.user))}
  function searchUsers(){const q=val("searchInput").trim().toLowerCase();renderUsers(!q?state.users:state.users.filter(u=>[u.fullname,u.phone,u.email,u.referral_code].some(v=>String(v||"").toLowerCase().includes(q))))}
  async function selectUser(id){state.user=state.users.find(u=>String(u.id)===String(id));if(!state.user)return;renderUsers();show("userProfileSection");show("machinesSection");renderProfile();await safe(loadUserMachines)}
  function renderProfile(){const u=state.user;if(!u)return;const s=(id,v)=>{if($(id))$(id).textContent=v};if($("profileAvatar"))$("profileAvatar").src=u.avatar_url||"images/default-avatar.png";s("profileName",u.fullname||"Unknown User");s("profilePhone",u.phone||"No phone number");s("profileEmail",u.email||"No email");s("membershipBadge",u.membership||"Standard");s("statusBadge",u.is_frozen?"Frozen":u.account_status||"Active");s("kycBadge",u.kyc_status||"Not Verified");s("profileWallet",money(u.wallet_balance));s("profileInvested",money(u.total_invested));s("profileProfit",money(u.total_profit));s("profileReferralBonus",money(u.total_referral_bonus));if($("freezeUserBtn"))$("freezeUserBtn").textContent=u.is_frozen?"✅ Unfreeze":"❄ Freeze"}

  async function loadUserMachines(){if(!state.user)return;const {data,error}=await db.from("user_machines").select("*").eq("user_id",state.user.id).order("purchase_date",{ascending:false});if(error)throw error;state.machines=data||[];renderMachineStats();renderMachines();if($("profileMachineCount"))$("profileMachineCount").textContent=state.machines.length}
  function renderMachineStats(){const a=state.machines.filter(m=>m.status==="active"&&!m.paused&&!m.completed).length,c=state.machines.filter(m=>m.completed||m.status==="completed").length,e=state.machines.filter(m=>m.status==="expired").length,p=state.machines.filter(m=>m.paused||m.status==="paused").length;[a,c,e,p].forEach((v,i)=>{const id=["activeMachineCount","completedMachineCount","expiredMachineCount","pausedMachineCount"][i];if($(id))$(id).textContent=v})}
  function daysLeft(m){if(m.remaining_days!==null&&m.remaining_days!==undefined)return Math.max(0,Number(m.remaining_days)||0);return m.expiry_date?Math.max(0,Math.ceil((new Date(m.expiry_date)-Date.now())/86400000)):0}
  function renderMachines(){const box=$("machinesList");if(!box)return;let list=state.machines.slice();if(state.filter==="vip")list=list.filter(m=>m.is_vip);if(state.filter==="active")list=list.filter(m=>m.status==="active"&&!m.paused&&!m.completed);if(state.filter==="paused")list=list.filter(m=>m.paused||m.status==="paused");if(state.filter==="completed")list=list.filter(m=>m.completed||m.status==="completed");if(!list.length){box.innerHTML='<div class="emptyCard"><h3>No Machines</h3><p>No machine assignments match this filter.</p></div>';return}box.innerHTML=list.map(m=>{const p=state.plans.find(x=>String(x.id)===String(m.machine_id)),d=Number(m.duration_days||p?.duration_days||0),cur=Number(m.current_day||0),pct=d?Math.min(100,Math.round(cur/d*100)):0,status=m.paused?"paused":m.status||(m.completed?"completed":"active");return `<article class="machineCard"><div class="machineTop"><img class="machineImage" src="${esc(m.machine_image||p?.image_url||"images/default-machine.png")}" alt=""><div class="machineInfo"><h3>${esc(m.machine_name||p?.name||"Mining Machine")}</h3><p>${esc(m.machine_series||p?.series||"Machine")}</p></div><span class="machineStatus ${esc(status)}">${esc(status.toUpperCase())}</span></div><div class="machineMeta"><span>${m.is_vip?"⭐ VIP":"Standard"}</span><span>${money(m.amount_paid)}</span><span>${daysLeft(m)} days left</span></div><div class="progressWrap"><div class="progressBar"><span style="width:${pct}%"></span></div><small>Day ${cur}/${d||"—"} • ${pct}%</small></div><div class="machineFinancials"><div><small>Daily</small><b>${money(m.daily_income)}</b></div><div><small>Earned</small><b>${money(m.earned_amount)}</b></div><div><small>Return</small><b>${money(m.total_return)}</b></div></div><div class="machineActions"><button class="secondaryBtn editMachine" data-id="${m.id}">✏ Edit</button><button class="secondaryBtn togglePause" data-id="${m.id}">${m.paused||status==="paused"?"▶ Resume":"⏸ Pause"}</button><button class="dangerBtn deleteMachine" data-id="${m.id}">🗑 Delete</button></div></article>`}).join("");box.querySelectorAll(".editMachine").forEach(b=>b.onclick=()=>openMachineModal(b.dataset.id));box.querySelectorAll(".togglePause").forEach(b=>b.onclick=()=>togglePause(b.dataset.id));box.querySelectorAll(".deleteMachine").forEach(b=>b.onclick=()=>deleteMachine(b.dataset.id))}
  function fillMachineSelect(){const s=$("machineSelect");if(!s)return;s.innerHTML='<option value="">Select Machine</option>'+state.plans.map(p=>`<option value="${p.id}">${esc(p.name)} — ${money(p.price)}${p.is_vip?" ⭐":""}</option>`).join("")}
  function openMachineModal(id=null){if(!state.user)return toast("Select a user first","error");state.editing=id?state.machines.find(m=>String(m.id)===String(id)):null;const m=state.editing,p=m?state.plans.find(x=>String(x.id)===String(m.machine_id)):null;if($("machineModalTitle"))$("machineModalTitle").textContent=m?"Edit Machine":"Assign Machine";setVal("machineSelect",m?.machine_id||"");setVal("machineStatus",m?.status||"active");setVal("machineVip",String(Boolean(m?.is_vip)));setVal("machineAmountPaid",m?.amount_paid??p?.price??0);setVal("machineDailyIncome",m?.daily_income??p?.daily_income??0);setVal("machineTotalReturn",m?.total_return??p?.total_return??0);setVal("machineEarnedAmount",m?.earned_amount??0);setVal("machinePurchaseDate",dateOnly(m?.purchase_date)||today());setVal("machineExpiryDate",dateOnly(m?.expiry_date));setVal("machineDurationDays",m?.duration_days??p?.duration_days??30);setVal("machineRemainingDays",m?.remaining_days??p?.duration_days??30);setVal("machineCurrentDay",m?.current_day??0);setVal("machineNotes",m?.admin_notes||m?.notes||"");setVal("customDays","");show("machineModal")}
  function applyPlanDefaults(){const p=state.plans.find(x=>String(x.id)===String(val("machineSelect")));if(!p||state.editing)return;setVal("machineAmountPaid",p.price);setVal("machineDailyIncome",p.daily_income);setVal("machineTotalReturn",p.total_return);setVal("machineDurationDays",p.duration_days);setVal("machineRemainingDays",p.duration_days);setVal("machineVip",String(Boolean(p.is_vip)));setVal("machinePurchaseDate",today());const d=new Date();d.setDate(d.getDate()+Number(p.duration_days||0));setVal("machineExpiryDate",d.toISOString().slice(0,10))}
  function adjustDays(n){const r=Math.max(0,Number(val("machineRemainingDays",0))+Number(n));setVal("machineRemainingDays",r);const d=new Date(`${val("machinePurchaseDate",today())}T00:00:00`);d.setDate(d.getDate()+r);setVal("machineExpiryDate",d.toISOString().slice(0,10));toast(`${n>=0?"Added":"Removed"} ${Math.abs(n)} day${Math.abs(n)===1?"":"s"}`)}
  async function saveMachine(){if(!state.user)return toast("Select a user first","error");const p=state.plans.find(x=>String(x.id)===String(val("machineSelect"))),old=state.editing;if(!p&&!old)return toast("Select a machine plan","error");const duration=Math.max(0,Number(val("machineDurationDays",p?.duration_days||0)));const purchase=val("machinePurchaseDate",today());let expiry=val("machineExpiryDate");if(!expiry&&duration){const d=new Date(`${purchase}T00:00:00`);d.setDate(d.getDate()+duration);expiry=d.toISOString().slice(0,10)}const status=val("machineStatus","active"),row={user_id:state.user.id,machine_id:val("machineSelect")||old?.machine_id,amount_paid:Number(val("machineAmountPaid",0)),purchase_date:iso(purchase),expiry_date:iso(expiry),status,machine_name:p?.name||old?.machine_name||"Mining Machine",machine_image:p?.image_url||old?.machine_image||null,is_vip:val("machineVip")==="true",earned_amount:Number(val("machineEarnedAmount",0)),completed:status==="completed",duration_days:duration,daily_income:Number(val("machineDailyIncome",0)),total_return:Number(val("machineTotalReturn",0)),machine_series:p?.series||old?.machine_series||null,current_day:Math.max(0,Number(val("machineCurrentDay",0))),remaining_days:Math.max(0,Number(val("machineRemainingDays",0))),paused:status==="paused",admin_notes:val("machineNotes").trim(),updated_at:new Date().toISOString()};const r=await safe(()=>old?db.from("user_machines").update(row).eq("id",old.id).select().single():db.from("user_machines").insert(row).select().single(),old?"Machine updated":"Machine assigned");if(r){hide("machineModal");state.editing=null;await safe(loadUserMachines)}}
  async function togglePause(id){const m=state.machines.find(x=>String(x.id)===String(id));if(!m)return;const pause=!(m.paused||m.status==="paused");const r=await safe(()=>db.from("user_machines").update({paused:pause,status:pause?"paused":"active",paused_at:pause?new Date().toISOString():null,resumed_at:pause?null:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("id",m.id),pause?"Machine paused":"Machine resumed");if(r)await safe(loadUserMachines)}
  async function deleteMachine(id){const m=state.machines.find(x=>String(x.id)===String(id));if(!m||!confirm("Delete this user's machine assignment? This cannot be undone."))return;const r=await safe(()=>db.from("user_machines").delete().eq("id",m.id),"Machine deleted");if(r)await safe(loadUserMachines)}
  function openEditUser(){const u=state.user;if(!u)return toast("Select a user first","error");show("editUserModal");[["editFullname",u.fullname],["editPhone",u.phone],["editEmail",u.email],["editCountry",u.country],["editGender",u.gender],["editMembership",u.membership||"Standard"],["editLevel",u.level||1],["editAccountStatus",u.account_status||"active"],["editKycStatus",u.kyc_status||"Not Verified"],["editReferralCode",u.referral_code]].forEach(([id,v])=>setVal(id,v))}
  async function saveUser(){if(!state.user)return toast("Select a user first","error");const u={fullname:val("editFullname").trim(),phone:val("editPhone").trim(),email:val("editEmail").trim(),country:val("editCountry").trim(),gender:val("editGender"),membership:val("editMembership"),level:Number(val("editLevel",1)),account_status:val("editAccountStatus","active"),kyc_status:val("editKycStatus","Not Verified"),referral_code:val("editReferralCode").trim(),updated_at:new Date().toISOString()};const r=await safe(()=>db.from("profiles").update(u).eq("id",state.user.id),"User updated");if(r){Object.assign(state.user,u);hide("editUserModal");renderUsers();renderProfile()}}
  async function toggleFreeze(){if(!state.user)return toast("Select a user first","error");const f=!Boolean(state.user.is_frozen);const r=await safe(()=>db.from("profiles").update({is_frozen:f,updated_at:new Date().toISOString()}).eq("id",state.user.id),f?"Account frozen":"Account unfrozen");if(r){state.user.is_frozen=f;renderUsers();renderProfile();updateOverview()}}
  async function resetPassword(){if(!state.user?.email)return toast("This user has no email address","error");await safe(()=>db.auth.resetPasswordForEmail(state.user.email,{redirectTo:`${location.origin}/reset-password.html`}),"Password reset email sent");hide("resetPasswordModal")}

  function bind(){
    $("refreshBtn")?.addEventListener("click",()=>safe(async()=>{await Promise.all([loadUsers(),loadPlans()]);if(state.user)await loadUserMachines()},"Data refreshed"));$("refreshMachinesBtn")?.addEventListener("click",()=>safe(loadUserMachines,"Machines refreshed"));$("searchInput")?.addEventListener("input",searchUsers);$("searchBtn")?.addEventListener("click",searchUsers);
    $("assignMachineBtn")?.addEventListener("click",()=>openMachineModal());$("saveMachineBtn")?.addEventListener("click",saveMachine);$("closeMachineModal")?.addEventListener("click",()=>{hide("machineModal");state.editing=null});$("cancelMachineBtn")?.addEventListener("click",()=>{hide("machineModal");state.editing=null});$("deleteMachineBtn")?.addEventListener("click",()=>state.editing&&deleteMachine(state.editing.id));$("machineSelect")?.addEventListener("change",applyPlanDefaults);
    document.querySelectorAll(".addDay,.removeDay").forEach(b=>b.addEventListener("click",()=>adjustDays(Number(b.dataset.days))));$("applyCustomDays")?.addEventListener("click",()=>{const n=Number(val("customDays",0));if(!n)return toast("Enter a non-zero number","error");adjustDays(n);setVal("customDays","")});
    $("editUserBtn")?.addEventListener("click",openEditUser);$("saveUserBtn")?.addEventListener("click",saveUser);$("freezeUserBtn")?.addEventListener("click",toggleFreeze);$("resetPasswordBtn")?.addEventListener("click",()=>show("resetPasswordModal"));$("closeResetPasswordModal")?.addEventListener("click",()=>hide("resetPasswordModal"));$("confirmResetPasswordBtn")?.addEventListener("click",resetPassword);$("cancelEditUserBtn")?.addEventListener("click",()=>hide("editUserModal"));$("closeEditUserModal")?.addEventListener("click",()=>hide("editUserModal"));
    document.querySelectorAll(".filterBtn").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".filterBtn").forEach(x=>x.classList.remove("active"));b.classList.add("active");state.filter=b.dataset.filter||"all";renderMachines()}));$("logoutBtn")?.addEventListener("click",async()=>{localStorage.removeItem("admin_bridge_token");localStorage.removeItem("admin_logged_in");await db.auth.signOut();location.replace("admin-login.html")});
    $("homeBtn")?.addEventListener("click",()=>location.href="admin.html");$("usersBtn")?.addEventListener("click",()=>location.href="admin-users.html");$("machinesBtn")?.addEventListener("click",()=>location.href="admin-machines.html");$("supportBtn")?.addEventListener("click",()=>location.href="admin-support.html");$("settingsBtn")?.addEventListener("click",()=>location.href="admin.html#settings");
    window.addEventListener("click",e=>{if(e.target===$("machineModal")){hide("machineModal");state.editing=null}if(e.target===$("editUserModal"))hide("editUserModal");if(e.target===$("resetPasswordModal"))hide("resetPasswordModal")});
  }
  async function init(){if(!adminAuth())return;bind();hide("userProfileSection");hide("machinesSection");await safe(async()=>{await Promise.all([loadUsers(),loadPlans()])})}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init();
})();