const db=window.supabaseClient;let currentUser=null,rows=[],filter='all';const $=id=>document.getElementById(id);const money=v=>'UGX '+Number(v||0).toLocaleString();const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));function kind(x){return String(x.type||x.source||x.category||'machine').toLowerCase()}function render(){const a=filter==='all'?rows:rows.filter(x=>kind(x).includes(filter));$('machineList').innerHTML=a.length?a.map(x=>'<div class="row"><div class="ico">'+(kind(x).includes('referral')?'👥':kind(x).includes('bonus')?'🎁':'⚡')+'</div><div class="main"><b>'+esc(x.title||x.description||x.source||'Machine Income')+'</b><small>'+new Date(x.created_at).toLocaleString()+'</small></div><div class="amt">+'+money(x.amount)+'</div></div>').join(''):'<div class="empty">No income records found.</div>'}function renderMachines(ms){const box=$('activeMachineList');if(!ms.length){box.innerHTML='<div class="machine-card"><div class="empty">No purchased machines found.</div></div>';return}const sorted=[...ms].sort((a,b)=>(a.completed?1:0)-(b.completed?1:0));box.innerHTML=sorted.map(m=>{const price=Number(m.amount_paid||0),total=Number(m.total_return||0),earned=Number(m.earned_amount||0),remaining=Math.max(total-earned,0),daily=Number(m.daily_income||0),duration=Number(m.duration_days||0),left=Number(m.remaining_days??Math.max(duration-Number(m.current_day||0),0));const pct=total?Math.max(0,Math.min(100,earned/total*100)):0;const st=String(m.status||'active').toLowerCase(),paused=Boolean(m.paused)||st.includes('weekend')||st.includes('pause'),done=Boolean(m.completed)||st==='completed'||st==='expired';const img=m.machine_image||'';const name=m.machine_name||'Mining Machine',series=m.machine_series||'';return '<article class="machine-card '+(done?'completed':'')+'"><div class="machine-top">'+(img?'<img class="machine-img" src="'+esc(img)+'" alt="'+esc(name)+'" loading="lazy">':'<div class="machine-img no-img">⚡</div>')+'<div class="machine-name"><h3>'+esc(name)+'</h3><small>'+esc(series||'Marathon Mining Machine')+'</small><span class="badge">'+(m.is_vip?'VIP • ':'')+'PURCHASED</span></div></div><div class="machine-grid"><div class="machine-stat"><span>Purchase Price</span><strong>'+money(price)+'</strong></div><div class="machine-stat"><span>Earned</span><strong class="green">'+money(earned)+'</strong></div><div class="machine-stat"><span>Remaining</span><strong>'+money(remaining)+'</strong></div><div class="machine-stat"><span>Total Return</span><strong>'+money(total)+'</strong></div><div class="machine-stat"><span>Daily Income</span><strong class="green">'+money(daily)+'</strong></div><div class="machine-stat"><span>Working Days Left</span><strong>'+left+' Days</strong></div></div><div class="progress-wrap"><div class="progress-label"><span>Earning progress</span><span>'+pct.toFixed(0)+'%</span></div><div class="progress"><i style="width:'+pct+'%"></i></div></div><div class="machine-status"><span>Status</span><span class="status '+(done?'done':paused?'pause':'active')+'">'+(done?'✓ COMPLETED':paused?'🟡 WEEKEND PAUSE':'🟢 ACTIVE')+'</span></div><div class="muted" style="margin-top:8px;font-size:9px">Purchased '+(m.purchase_date?new Date(m.purchase_date).toLocaleString():'')+'</div></article>'}).join('')}async function load(){try{const s=await db.auth.getUser();currentUser=s.data.user;if(!currentUser){location.href='login.html';return}const r=await db.from('wallet_transactions').select('*').eq('user_id',currentUser.id).order('created_at',{ascending:false});if(r.error)throw r.error;rows=(r.data||[]).filter(x=>Number(x.amount||0)>0);const total=rows.reduce((n,x)=>n+Number(x.amount||0),0);$('totalIncome').textContent=money(total);$('recordCount').textContent=rows.length;const d=new Date();d.setHours(0,0,0,0);$('dailyIncome').textContent=money(rows.filter(x=>new Date(x.created_at)>=d).reduce((n,x)=>n+Number(x.amount||0),0));const m=await db.from('user_machines').select('id,machine_id,amount_paid,purchase_date,expiry_date,status,machine_name,machine_image,is_vip,earned_amount,completed,duration_days,daily_income,total_return,machine_series,current_day,remaining_days,paused,paused_at,updated_at').eq('user_id',currentUser.id).order('purchase_date',{ascending:false});if(m.error)throw m.error;const machines=m.data||[];const active=machines.filter(x=>!x.completed&&String(x.status||'active').toLowerCase()!=='completed'&&String(x.status||'active').toLowerCase()!=='expired');$('activeMachines').textContent=active.length;renderMachines(machines);const p=await db.from('profiles').select('total_invested,total_profit').eq('id',currentUser.id).maybeSingle();$('totalReturn').textContent=money(p.data?.total_profit);render()}catch(e){console.error('Income page:',e);$('machineList').innerHTML='<div class="empty">Unable to load income records.</div>';renderMachines([])}}document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.f;render()});load();setInterval(load,30000);