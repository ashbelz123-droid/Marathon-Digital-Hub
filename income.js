const db=window.supabaseClient;let currentUser=null,rows=[],filter='all';const $=id=>document.getElementById(id);const money=v=>'UGX '+Number(v||0).toLocaleString();function kind(x){return String(x.type||x.source||x.category||'machine').toLowerCase()}function render(){const a=filter==='all'?rows:rows.filter(x=>kind(x).includes(filter));$('machineList').innerHTML=a.length?a.map(x=>'<div class="row"><div class="ico">'+(kind(x).includes('referral')?'👥':kind(x).includes('bonus')?'🎁':'⚡')+'</div><div class="main"><b>'+String(x.title||x.description||x.source||'Machine Income')+'</b><small>'+new Date(x.created_at).toLocaleString()+'</small></div><div class="amt">+'+money(x.amount)+'</div></div>').join(''):'<div class="empty">No income records found.</div>'}async function load(){try{const s=await db.auth.getUser();currentUser=s.data.user;if(!currentUser){location.href='login.html';return}const r=await db.from('wallet_transactions').select('*').eq('user_id',currentUser.id).order('created_at',{ascending:false});if(r.error)throw r.error;rows=(r.data||[]).filter(x=>Number(x.amount||0)>0);const total=rows.reduce((n,x)=>n+Number(x.amount||0),0);$('totalIncome').textContent=money(total);$('recordCount').textContent=rows.length;const d=new Date();d.setHours(0,0,0,0);$('dailyIncome').textContent=money(rows.filter(x=>new Date(x.created_at)>=d).reduce((n,x)=>n+Number(x.amount||0),0));const m=await db.from('user_machines').select('id,status,completed').eq('user_id',currentUser.id);if(!m.error)$('activeMachines').textContent=(m.data||[]).filter(x=>x.completed!==true&&x.status!=='completed').length;const p=await db.from('profiles').select('total_invested,total_profit').eq('id',currentUser.id).maybeSingle();$('totalReturn').textContent=money(p.data?.total_profit);render()}catch(e){console.error(e);$('machineList').innerHTML='<div class="empty">Unable to load income records.</div>'}}document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.f;render()});load();setInterval(load,30000);